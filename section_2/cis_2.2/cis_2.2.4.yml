---

{{ if .Vars.rhel10cis_level_1 }}
  {{ if not .Vars.rhel10cis_tftp_client }}
    {{ if .Vars.rhel10cis_rule_2_2_4 }}
package:
  tftp_client:
    title: 2.2.4 | Ensure tftp client is not installed
    installed: false
    name: tftp
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.2.4
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
